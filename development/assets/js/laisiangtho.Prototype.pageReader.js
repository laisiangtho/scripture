Core.prototype.pagereader = function() {
    new this.content(fO.query).chapter(fO.container.main.children());
    // new this.content(fO.query).chapter(fO.container.main.children());
};
Core.prototype.pagelookup = function() {
    // new this.content(fO.query).lookup(fO.container.main.children());
    new this.content(fO.query).lookup(fO.container.main.children());
};
